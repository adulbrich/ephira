// Formatting and linting. Pinned: a Biome release that changes a formatting
// default would produce exactly the churn this migration avoided.
{
  "$schema": "https://biomejs.dev/schemas/2.5.9/schema.json",
  "vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
  "files": {
    "includes": [
      "**",
      "!**/node_modules",
      "!drizzle",
      "!docs",
      "!dist",
      "!.expo",
      "!android",
      "!ios",
      "!credentials"
    ]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 80
  },
  "javascript": {
    "formatter": { "quoteStyle": "double", "trailingCommas": "all" },
    "globals": [
      "__DEV__",
      "ErrorUtils",
      "HermesInternal",
      "nativeFabricUIManager"
    ]
  },
  "linter": {
    "enabled": true,
    "domains": { "react": "recommended" },
    "rules": {
      "preset": "recommended",
      "suspicious": {
        // On since #228 burned down all 25 sites. Two of them were hiding
        // real defects rather than being merely lazy: nine `as any` route
        // casts were silencing a stale generated `.expo/types/router.d.ts`
        // and disabling route checking to do it, and `DayData` was declaring
        // four nullable columns non-null.
        "noExplicitAny": "error"
      },
      "correctness": {
        // reportUnnecessaryDependencies is off deliberately. `databaseChange`
        // is a store signal bumped on every write and listed in effect deps
        // purely to invalidate; it is never read in the body, which is the
        // point. Biome calls it unnecessary in four screens and offers an
        // autofix that deletes it, which would silently kill refresh-on-write.
        "useExhaustiveDependencies": {
          "level": "error",
          "options": { "reportUnnecessaryDependencies": false }
        }
      }
    }
  },
  "assist": { "actions": { "source": { "organizeImports": "off" } } }
}
